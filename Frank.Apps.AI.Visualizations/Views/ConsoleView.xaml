<Window x:Class="Frank.Apps.AI.Visualizations.Views.ConsoleView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Frank.Apps.AI.Visualizations.Views"
        mc:Ignorable="d"
        WindowStyle="ToolWindow"
        ResizeMode="NoResize"
        Title="ConsoleView" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="0.05*" />
        </Grid.RowDefinitions>

        <ListBox Name="ConsoleOutput" DataContext="{Binding Output}"  Grid.Column="0" Grid.Row="0" FontFamily="Lucida Console" Background="Black" Foreground="Chartreuse" Padding="7,0,7,0" VerticalContentAlignment="Bottom"/>
        <TextBox Name="ConsoleInput" Grid.Column="0" Grid.Row="1" FontFamily="Lucida Console" Background="Black" Foreground="Chartreuse" />

    </Grid>
</Window>
